using UnityEngine;

public class SkillEnum : MonoBehaviour {

    // Enum um Fähigkeiten der Orbs auszuwählen. MUSS FÜR JEDE FÄHIGKEIT ERWEITERT WERDEN
    public enum Skill { SKILLEMPTY, SKILLSLASH, SKILLBAT, SKILLFIRE, SKILLMACE };
}
